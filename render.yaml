services:
  - type: worker
    name: lead-gen-engine-bot
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.13
      - key: TELEGRAM_BOT_TOKEN
        sync: false
